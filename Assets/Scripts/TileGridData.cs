using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileGridData 
{
    // List<TileSerializableKeyValuePair> serialzableDict = new List<TileSerializableKeyValuePair>();
    // public TileGridData(TileStorage tiles) {
    //     foreach (var kvp in tiles)
    //     {
    //         serializableDict.Add(new SerializableKeyValuePair { key = kvp.Key, value = kvp.Value });
    //     }
    // }

}
